# import User, Message, Channel, Query.*, Subscription.*, Mutation.createChannel from "./Remotes/Core.graphql"

type Mutation {
  signup(email: String!): User!
  deactivateByEmail(email: String!): User!
  sendMessage(
    content: String!
    authorId: ID!
    channelId: ID!
    sentAt: DateTime!
  ): Message!
}
