# import User, Message, Channel, Query.*, Subscription.* from "./Remotes/Core.graphql"

# TODO do not expose Query.node

type Mutation {
  signup(email: String!): User!
  deactivateByEmail(email: String!): User!
  sendMessage(content: String!): String
}
